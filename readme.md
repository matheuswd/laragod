# Basic Installation

$ composer install

$ npm i

# Laravel Mix

$ npm run dev

or

$ npm run build

# Helpers

$ composer dump-autoload

$ php artisan views:clear

$ php artisan cache:clear

$ php artisan migrate

$ php artisan key:generate
